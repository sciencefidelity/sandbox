# Python

```shell
# run project
python run

# start the app
# python main.py && fswatch -o . | (while read; do python main.py; done)
pywatch

# run the tests

# start the test suite

# format code
```
